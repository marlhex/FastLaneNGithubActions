//
//  ContentView.swift
//  FastLaneNGithubActions
//
//  Created by Ignacio Arias on 2024-01-13.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, this is Marlhex!")
            Text("Welcome")
            Text("Adding fastline files to github")
            Text("Github Actions")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
